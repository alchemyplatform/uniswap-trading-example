const BetterUniswap = artifacts.require("BetterUniswap");

contract("Better Uniswap", async (accounts) => {
  it("Test insufficient funds", async () => {
    const betterUniswapInstance = await BetterUniswap.deployed();
  });
  it("Test pecentage fee", async () => {
    const betterUniswapInstance = await BetterUniswap.deployed();
  });
  it("Test trade route", async () => {
    const betterUniswapInstance = await BetterUniswap.deployed();
  });
});
